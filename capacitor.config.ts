import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sistemacolinareal.app',
  appName: 'Sistema Colina Real',
  webDir: 'dist/sistema-colina-real/browser'
};

export default config;
